export type FestivalInfo = {
  id: number;
  pic_url: string;
  title: string;
  expired_at: string;
  url: string | null;
  button_text: string;
  show_button: boolean;
  show_count_down: boolean;
} | null;
